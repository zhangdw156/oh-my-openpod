{
  "$schema": "https://opencode.ai/config.json",

  // ----------------------------------------------------------------
  // openpod 镜像内置的 OpenCode 全局默认配置：
  // - 仓库维护的全局 skills 通过 /root/.config/opencode/skills 软链接暴露
  // superpowers 自带 skills 由其插件在运行时自动注册，这里不要重复添加。
  // provider、模型和凭据由用户自行在 OpenCode 配置中维护。
  // ----------------------------------------------------------------
  "provider": {}
}
