#!/usr/bin/env bash
set -euo pipefail

real_bin="${OPENPOD_COPILOT_REAL_BIN:-${OPENPOD_BIN_DIR:-/usr/local/bin}/copilot-real}"
if [[ -z "${real_bin}" || ! -x "${real_bin}" ]]; then
  echo "copilot real binary not found: ${real_bin:-unset}" >&2
  exit 1
fi

exec "${real_bin}" "$@"
